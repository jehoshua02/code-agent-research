# Local-agents picking history

Personal working notes — dated stack picks tied to specific moments in the local-agents project. Use this directory to record:

- Picks for your own machine and goals
- Updates after trying a stack ("retrospective: OpenCode + Ollama after 2 weeks")
- Re-picks when constraints change (hardware upgrade, new task, license preference shift)

Suggested filename: `YYYY-MM-DD-<scenario-slug>.md`.

## Worked examples (tool documentation, not personal history)

For canonical examples of how to use `query.py`, see [`scripts/examples/`](../../../scripts/examples/) instead. Those demonstrate the tooling; this directory is for your own picks.
